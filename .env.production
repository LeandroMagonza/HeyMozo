# Database Configuration
DB_NAME=heymozo
DB_USER=postgres
DB_PASSWORD=1Q.2w.3e.4r.
DB_HOST=localhost
DB_PORT=5432

# JWT Authentication
JWT_SECRET=your-secure-development-jwt-secret

# API Configuration
# In production, REACT_APP_API_URL is NOT set so it defaults to '/api' (relative URL)
# This works because the React build is served by the same Express server

# Node Environment
NODE_ENV=production

# Email Configuration (for testing - will log to console)
EMAIL_FROM=no-reply@heymozo.com